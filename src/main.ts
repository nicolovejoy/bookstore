/**


Main()
    Load File - request to create and/or update records
    Validate CSV format (need )
    See if records need to be inserted
    See if records need to be updated
    report back in some fashion
  

Write some code! Let's:

Code to read a file (wait on this for now)

Code to grab header 

Code to validate syntax of rows 2-n in files

Code to check if our "database" needs updating for each new submission





    */
const header = "title, author, isbn, publisher, date";
export const checkHeaders = (header: string) => {
  return true;
};

export const sum = (...a: number[]) => a.reduce((acc, val) => acc + val, 0);
